import "babel-polyfill";
import { Guest } from "../src/Classes/Guest";

var assert = require("chai").assert;

describe("Guest", () => {
  it("exists", () => {});

  describe("properties", () => {
    it("avatar");
    it("chat_id");
    it("email");
    it("first_name");
    it("last_name");
  });
});
