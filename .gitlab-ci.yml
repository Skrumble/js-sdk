image: node:latest

before_script:
  - rm -rf yarn.lock
  - rm -rf node_modules/
  - npm install

pages:
  stage: deploy
  script:
  - npm run docs
  - npm run docs_dash
  artifacts:
    paths:
    - public
  only:
  - master
